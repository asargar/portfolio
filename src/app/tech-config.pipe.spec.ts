import { TechConfigPipe } from './tech-config.pipe';

describe('TechConfigPipe', () => {
  it('create an instance', () => {
    const pipe = new TechConfigPipe();
    expect(pipe).toBeTruthy();
  });
});
