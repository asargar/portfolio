{
    "name": "Node.js",
    "build": {
      "dockerfile": "Dockerfile"
    },
    "customizations": {
      "vscode": {
        "extensions": [
          "ms-vscode-remote.remote-containers"
        ],
        "settings": {
          "terminal.integrated.shell.linux": "/bin/bash"
        }
      }
    },
    "postCreateCommand": "nvm install 10.24.1 && nvm use 10.24.1 && npm install -g @angular/cli@~8.3.20 && npm install"
  }
  